@model Round
@{
}

<div>
    <h3>@Model.Name</h3>
    @for (int i = 0; i < @Model.Questions.Count; i++)
    {
        <p>@(i + 1). @Model.Questions[i].Text</p>
        <p>A: @Model.Questions[i].Answer</p>
    }
</div>