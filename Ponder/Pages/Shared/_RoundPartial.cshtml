@model Round
@{
}

<div>
    <h3>@Model.Name</h3>
    @for (int i = 0; i < @Model.Questions.Count; i++)
    {
        <p>@(i + 1). @Model.Questions[i].Text <em>A: @Model.Questions[i].Answer</em></p>
    }
</div>